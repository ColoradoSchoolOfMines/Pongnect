<Window x:Class="ACMX.Games.Pongnect.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Loaded="WindowLoaded"
        WindowState="Maximized" WindowStyle="None"
        Title="MainWindow" Height="350" Width="525">

    <Grid Name="layoutGrid" Margin="0 0 0 0" Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Viewbox Grid.RowSpan="3" Grid.ColumnSpan="2" Stretch="Uniform">
            <Image Name="Canvas" Stretch="Uniform"/>
        </Viewbox>
        <TextBox Foreground="Gray" Background="Transparent" TextAlignment="Left" BorderBrush="Transparent" Name="LivesText" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" FontSize="50">B</TextBox>
        <TextBox Foreground="Gray" Background="Transparent"  TextAlignment="Right" BorderBrush="Transparent" Name="ScoreText" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" FontSize="50">F</TextBox>
    </Grid>
</Window>
